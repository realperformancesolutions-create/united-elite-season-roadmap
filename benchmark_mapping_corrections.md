# Benchmark Mapping Corrections Needed

The current `Home.tsx` roadmap data places several benchmark descriptions under the wrong benchmark label. The correction should use `benchmark_mapping_from_ppt.md` as the authoritative mapping.

| Current issue | Required correction from PowerPoint |
|---|---|
| BENCHMARK #2 currently shows `TEAMS NEED TO LEAVE...`, which is non-benchmark deck text. | BENCHMARK #2 must show `TEAM DRILLS` with `VIDEO DUE  7/13`. |
| BENCHMARK #1 currently uses `VIDEO DUE 7/13`. | BENCHMARK #1 must show `TUMBLING  SKILL SELECTION` with `OUTLINE DUE: 7/9`. |
| BENCHMARK #4 currently includes `WE HAVE TO GET IT RIGHT !`, which is separate deck text. | BENCHMARK #4 must show only `STUNT SEQUENCE`, `(ELEMENT BY ELEMENT)`, `CREATIVE IN 1-2 ELEMENTS`, `MID CREATIVE ELEMENT`, `FINAL 1-2 ELEMENTS & DISMOUNT`, and `VIDEO DUE:8/20`. |
| BENCHMARK #7 and BENCHMARK #10 currently mix flyer-flex/body-position wording. | BENCHMARK #7 must show flyer-flex measurable and body-position collage wording; BENCHMARK #10 must show only `FLYER BODY POSITION CHECK`. |
| BENCHMARK #9 currently combines initial pyramid concepts with full-out and no-exceptions text. | BENCHMARK #9 in the October section must show `FULL STUNT ISOLATION` with `VIDEO DUE:9/24`; initial pyramid concepts belongs to BENCHMARK #5, and full-out/no-exceptions belongs to BENCHMARK #11. |
| November BENCHMARK #9 currently includes rubric double-check wording. | November BENCHMARK #9 must show USASF legality submission only; rubric double-check must be a separate BENCHMARK #10 card. |
| BENCHMARK #12 / #13 pyramid text is shifted. | BENCHMARK #12 must show the first-half/second-half pyramid text; BENCHMARK #13 must show pyramid isolation/dance choreo with gym rehearsal. |
